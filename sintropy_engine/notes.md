# NOTES
- create validations when creating a producer, e.g. does the channel exists
- create validations when creating a messages, does channel, producer, routing-key exists
- compare validations with quarkus version

